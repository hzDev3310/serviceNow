import { Member } from '@vonage/conversations/dist/types/member';
import { UpdateMemberParameters } from '@vonage/conversations/dist/types/parameters/updateMemberParameters';
export type MessageMemberBody = {
    /**
     * Member ID
     */
    memberId: string;
    /**
     * Audio Message
     */
    audio?: boolean;
} & Omit<Member, 'status' | 'id' | 'conversationId'> & Omit<UpdateMemberParameters, 'state' | 'from'>;
