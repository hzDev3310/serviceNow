import { AbstractImageMessage } from '@vonage/messages/dist/classes/AbstractImageMessage';
import { WhatsAppImageParams } from '@vonage/messages/dist/types';
/**
 * Represents an image message for WhatsApp.
 *
 * @group WhatsApp
 */
export declare class WhatsAppImage extends AbstractImageMessage implements WhatsAppImageParams {
    channel: 'whatsapp';
    /**
     * Sends an image message to a WhatsApp user.
     *
     * @param {WhatsAppImageParams} params - The parameters for creating a WhatsApp image message.
     * @example
     * ```ts
     * import { WhatsAppImage } from '@vonage/messages';
     *
     * const { messageUUID } = await messagesClient.send(new WhatsAppImage({
     *  to: TO_NUMBER,
     *  from: FROM_NUMBER,
     *  image: {
     *    url: 'https://example.com/image.jpg',
     *    caption: 'This is an image message',
     *  },
     *  clientRef: 'my-personal-reference',
     * }));
     *
     * console.log(`Message sent successfully with UUID ${messageUUID}`);
     * ```
     */
    constructor(params: WhatsAppImageParams);
}
