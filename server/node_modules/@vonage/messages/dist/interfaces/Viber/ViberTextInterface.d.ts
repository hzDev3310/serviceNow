import { MessageTextInterface } from '@vonage/messages/dist/interfaces/MessageTextInterface';
import { ViberChannelInterface } from '@vonage/messages/dist/interfaces/Viber/ViberChannelInterface';
import { ViberActionParams } from '@vonage/messages/dist/types';
/**
 * Represents the Viber text interface.
 *
 * @deprecated Please use types instead of interfaces.
 *
 * @ignore
 */
export interface ViberTextInterface extends MessageTextInterface, ViberChannelInterface {
    /**
     * Represents Viber-specific action parameters.
     */
    viberService: ViberActionParams;
}
