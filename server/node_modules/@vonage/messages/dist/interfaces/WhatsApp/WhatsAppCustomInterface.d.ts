import { MessageInterface } from '@vonage/messages/dist/interfaces/MessageInterface';
import { WhatsAppChannelInterface } from '@vonage/messages/dist/interfaces/WhatsApp/WhatsAppChannelInterface';
import { WhatsAppCustomType } from '@vonage/messages/dist/types';
/**
 * Represents a WhatsApp custom message interface.
 *
 * @deprecated Please use types instead of interfaces.
 *
 * @ignore
 */
export interface WhatsAppCustomInterface extends MessageInterface, WhatsAppChannelInterface {
    /**
     * Custom data for the WhatsApp message.
     */
    custom: WhatsAppCustomType;
}
