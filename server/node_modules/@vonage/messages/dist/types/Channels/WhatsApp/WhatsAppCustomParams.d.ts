import { MessageParams } from '@vonage/messages/dist/types/MessageParams';
import { WhatsAppCustomType } from '@vonage/messages/dist/types/Channels/WhatsApp/WhatsAppCustomType';
/**
 * Represents parameters for sending a custom WhatsApp message with a file attachment.
 *
 * @group WhatsApp
 * @category Parameters
 */
export type WhatsAppCustomParams = {
    /**
     * The custom WhatsApp message type.
     */
    custom: WhatsAppCustomType;
} & MessageParams;
