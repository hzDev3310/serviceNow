import { MessageParams } from '@vonage/messages/dist/types/MessageParams';
import { MessageImageType } from '@vonage/messages/dist/types/MessageImageType';
/**
 * Represents the parameters for a message with an image attachment.
 */
export type MessageParamsImage = {
    /**
     * The image attachment content.
     */
    image: MessageImageType;
} & MessageParams;
